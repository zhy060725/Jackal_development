# Agent 工作约束

本文件用于约束 Codex/AI coding agent 在本仓库中的工作方式。仓库目标是基于 Jackal 小车和 ROS2 Foxy 系统，开发小车运动控制、目标检测、目标追踪、避障等相关功能中的 Python 代码。

## 项目定位

- 本仓库仅用于 ROS2 Foxy 系统中 Python 代码的撰写、维护和验证。
- 代码应服务于 Jackal 小车相关功能，包括但不限于运动控制、目标检测、目标追踪、避障策略、节点间数据处理和工具脚本。
- Agent 不应在没有明确要求时引入非 Python 组件、非 ROS2 运行框架或与本项目目标无关的工程结构。
- 所有实现应优先适配 ROS2 Foxy 的能力和接口约束，避免使用仅在更新 ROS2 版本中可用的 API。

## Agent 角色与边界

- Agent 是本仓库的 Python 代码协作者，负责阅读上下文、提出实现方案、编写代码、补充必要测试并说明验证结果。
- Agent 在修改代码前应先了解现有目录结构、包结构、节点职责、消息接口和已有约定。
- Agent 应保持改动范围清晰，不进行与当前任务无关的重构、格式化或文件搬迁。
- Agent 不应假设硬件、传感器、话题名称、坐标系名称或启动方式已经存在；缺少信息时应通过代码上下文确认，必要时向用户询问。
- Agent 应优先保持代码可读、可调试、可在 ROS2 节点中稳定运行，而不是追求复杂抽象。

## 工作流程

1. 读取上下文
   - 优先检查仓库结构、已有 README、package 配置、节点代码、launch 文件和测试。
   - 使用 `rg`、`rg --files` 等快速检索工具查找相关实现。

2. 明确任务边界
   - 先确认本次任务属于运动控制、目标检测、目标追踪、避障、数据处理、测试或工具脚本中的哪一类。
   - 若任务涉及实车运动、安全距离、速度限制、传感器输入或控制输出，应明确安全约束后再实现。

3. 小步实现
   - 每次修改应围绕一个清晰目标展开。
   - 优先复用已有节点、工具函数、参数命名和消息处理模式。
   - 避免一次性引入大规模架构变化。

4. 验证结果
   - 修改 Python 文件后，至少运行语法级检查，例如 `python3 -m py_compile`。
   - 若仓库中已有测试，应运行相关测试。
   - 若无法运行 ROS2、仿真或硬件相关验证，应在结果说明中明确未验证项和原因。

5. 汇报交付
   - 说明修改了哪些文件。
   - 说明实现了什么行为。
   - 说明运行了哪些验证命令。
   - 对未验证的硬件、传感器或 ROS2 运行时行为保持明确标注。

6. 文档维护
   - 涉及系统设计、接口决策或模块边界变化时，应同步更新 `PROJECT_LOG.md`。
   - 涉及 ROS2 package、依赖、构建、部署或小车主机迁移方式变化时，应同步更新安装指南。
   - 文档中的已确认决策和待确认问题应分开记录，避免把未确认假设写成事实。

## 技术栈约束

- ROS 版本：ROS2 Foxy。
- 主要语言：Python。
- ROS2 Python 客户端库：`rclpy`。
- 节点代码应遵循 ROS2 Python package 的常规组织方式。
- 不应默认引入需要额外系统安装的大型依赖；如确有必要，应说明用途、版本约束和替代方案。
- 与目标检测、追踪相关的模型推理代码应与 ROS2 节点逻辑保持适度分离，避免节点主循环中堆积不可测试的复杂逻辑。

## 编码规范

- Python 代码应清晰、直接，优先使用标准库和项目已有依赖。
- 新增 package、节点、topic、参数、message、文件或测试时，应遵循 `docs/naming_conventions.md`。
- 命名应表达 ROS2 语义：
  - 节点类使用清晰的 PascalCase 名称。
  - 话题、参数、frame、消息字段相关变量使用描述性 snake_case 名称。
  - 回调函数命名应体现输入来源或行为，例如 `scan_callback`、`image_callback`、`timer_callback`。
- ROS2 节点应尽量保持职责单一：
  - 运动控制节点负责速度指令生成和发布。
  - 感知节点负责传感器输入解析、检测结果生成或转换。
  - 追踪节点负责目标状态维护。
  - 避障节点负责安全约束、距离判断和控制修正。
- 参数应通过 ROS2 参数机制声明和读取，不应把速度、距离阈值、置信度阈值、话题名等关键配置硬编码在逻辑深处。
- 发布器、订阅器、定时器和参数声明应集中在节点初始化阶段，便于检查节点接口。
- 回调函数中应避免长时间阻塞操作；耗时推理、图像处理或路径计算应明确控制频率，必要时拆分逻辑。
- 对外发布控制指令时，应显式处理异常输入、空数据、超时数据和传感器失效场景。
- 速度控制相关代码应保守处理默认值；当输入无效或状态未知时，默认行为应倾向于停止或不发布危险指令。
- 日志应使用 ROS2 logger，不应使用大量裸 `print`。
- 日志内容应能辅助调试节点状态、输入数据质量、控制输出和异常原因。
- 类型标注应在关键函数、数据结构和工具函数中使用，提升可读性和静态检查价值。
- 注释应解释非显然的控制策略、坐标变换、滤波逻辑或安全判断，不应重复代码表面含义。
- 代码应便于单元测试；复杂判断逻辑应尽量抽成纯函数，避免全部写入 ROS2 回调中。

## 安全相关禁止事项

- 禁止在未确认安全约束的情况下生成可能驱动实车高速运动的代码。
- 禁止默认发布非零速度指令作为节点启动后的初始行为。
- 禁止在传感器数据缺失、过期、异常或无法解析时继续执行危险运动。
- 禁止绕过急停、限速、最小安全距离等已有安全机制。
- 禁止把最大线速度、最大角速度、避障距离阈值等安全参数写死为不可配置常量。
- 禁止为了通过测试而删除、弱化或跳过安全检查逻辑。
- 禁止在没有用户明确要求和充分上下文的情况下修改系统级配置、硬件驱动配置、实车启动脚本或网络配置。
- 禁止假设仿真环境中的安全表现可以直接代表实车表现；涉及实车运行的结论必须明确标注验证条件。
